target/
# Default local avatar storage dir (app.avatars.storage-dir) — created on
# startup, holds uploaded images when running outside the prod container.
data/
.idea/
*.iml
.vscode/
.classpath
.project
.settings/
.DS_Store

# Wrapper distribution jar (script-only distribution, nothing to ignore
# beyond what's already excluded) — kept explicit for clarity.
.mvn/wrapper/maven-wrapper.jar
